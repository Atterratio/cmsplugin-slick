{% load cms_tags sekizai_tags staticfiles %}

<div class="carousel-slick-plugins-wrapper {{ instance.classes }}">
	{% for plugin in instance.child_plugin_instances %}
		{% render_plugin plugin %}
  {% endfor %}
</div>
